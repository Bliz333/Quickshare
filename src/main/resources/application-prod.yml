spring:
  datasource:
    url: jdbc:mysql://localhost:19820/quickshare_sql?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&connectTimeout=60000&socketTimeout=60000
    username: quickshare_user
    password: PFV6KeWUtfftD7cxt4KH
    hikari:
      connection-timeout: 60000
      idle-timeout: 600000
      max-lifetime: 1800000

  servlet:
    multipart:
      max-file-size: 2GB
      max-request-size: 2GB
      file-size-threshold: 10MB

  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 60000

  mail:
    host: smtp.gmail.com
    port: 587
    username: enzo.han928@gmail.com
    password: zhwujbyryhrjlfxt
    properties:
      mail:
        smtp:
          auth: true
          timeout: 60000
          connection-timeout: 60000
          starttls:
            enable: true
            required: true

server:
  port: 8888
  tomcat:
    connection-timeout: 600000          # 10分钟超时
    max-swallow-size: -1                # 不限制
    max-http-form-post-size: -1         # 不限制表单大小
    threads:
      max: 200
      min-spare: 10
  max-http-header-size: 8KB

file:
  upload-dir: /opt/quickshare/uploads