# QuickShare 项目状态（2026-03-18）

## 当前完成情况
- 后端：Spring Boot 3.2 + MyBatis-Plus；支持注册/登录（JWT）、邮箱验证码（Redis 缓存）、基础文件上传/预览/下载、分享链接创建与校验、文件/文件夹的新增、删除、重命名（部分）。
- 前端：静态页 index/login/register/netdisk，包含上传与分享流程、多语言与主题切换、网盘文件展示/搜索/本地缓存。部分交互尚未与后端完全对齐（见下方缺陷）。
- 安全加固（进行中）：引入 Spring Security + JWT 过滤、全局异常处理；配置改为环境变量占位并提供 `application-local.example.yml`；上传校验文件名/类型/大小。

## 进度表
| 阶段 | 状态 | 当前判断 | 下一步 |
| --- | --- | --- | --- |
| 阶段 0：基线与配置清理 | 基本完成 | 敏感配置已改为环境变量占位，提供了本地示例配置。 | 清理 README 中仍然滞后的配置说明。 |
| 阶段 1：安全与鉴权强化 | 进行中 | Spring Security、JWT 过滤、全局异常、reCAPTCHA 与验证码限流已接入，但角色权限、CORS 收紧、统一异常落地仍未完全收口。 | 补接口级鉴权回归测试，去掉控制器层零散 `try/catch`。 |
| 阶段 2：文件夹与文件闭环 | 进行中 | `/api/files?folderId=`、上传 `folderId`、文件夹重命名、重名冲突校验、物理文件清理以及网盘分享参数对齐已补上，目录主链路已基本连通。 | 继续收口更深层目录联调、逻辑删除一致性和页面缓存边界。 |
| 阶段 3：管理员体系与后台 | 未开始 | 仅在 JWT 中预留了角色字段，暂无管理员接口与后台入口。 | 先完成阶段 1-2 后再设计权限边界。 |
| 阶段 4：易支付接入 | 未开始 | 仅有规划，无订单模型、回调或签名验签实现。 | 阶段 1-3 稳定后再接入。 |
| 阶段 5：运维与质量 | 未开始 | 目前只有 `contextLoads` 启动测试，缺少迁移脚本、CI、监控与覆盖率。 | 从文件闭环开始补针对性测试。 |

## 当前执行框架
1. 先完成阶段 2 最小闭环：目录列表、文件夹重命名、上传归属。
2. 每完成一个小步就补测试并执行 `mvn test`。
3. 阶段 2 打通后，再回头收阶段 1 的鉴权与异常一致性。

## 主要缺陷与风险
- 配置安全：现已去除明文密钥，需按环境变量/本地 profile 正确注入，否则无法连接外部资源。
- 鉴权待补完：已移除 `userId=1` 回退并启用 JWT 过滤，但尚未区分角色，也未对分享/下载做频控；默认 CORS 仍可配置为 `*`，需按部署环境收紧。
- 文件夹/文件闭环仍需联调：目录列表、上传归属、文件夹重命名、重名冲突校验与物理删除已补齐，但更深层目录回归、前端缓存边界和逻辑删除一致性还未系统验证。
- 文件可靠性：上传基础校验与流关闭已处理，删除也会同步清理物理文件；但 `ShareLink.status`、分享唯一性、下载次数并发安全、MD5/写入流程优化仍待继续处理。
- 基础设施：缺少数据库建表/迁移脚本、自动化测试与全局异常处理，日志与监控较弱。
- 文档与协议：README 描述的特性与实现程度不完全一致；仓库缺少独立的 `LICENSE` 文件。

## 近期改进优先级（建议顺序）
1) 安全与配置：剥离明文密钥，改用 profile/env；恢复 reCAPTCHA 校验与发送频率限制；为 `/api/**` 启用统一的身份验证与异常处理，移除默认 `userId=1` 行为。
2) 文件/文件夹闭环：上传、列表、重命名、删除全链路支持 `folderId`；`/api/files` 支持按文件夹查询并过滤逻辑删除；补齐 `/folders/{id}/rename`；删除时同步清理物理文件。
3) 分享与预览：确保 `ShareLink` 唯一性与默认状态；下载/预览校验过期/次数/权限；为图片/视频增加缩略图缓存与限流。
4) 可运维性：引入 Flyway/Liquibase 建表，增加单元/集成测试（鉴权、上传、分享、文件夹），限制 CORS 域名，完善日志与全局错误返回格式。
5) 产品化：修订 README/前端文案与实际功能对齐，新增 LICENSE，补充部署与性能调优指引。

## 运行与配置提示
- 推荐新增 `application-local.yml`（不入库）并通过环境变量注入数据库、Redis、SMTP、JWT、reCAPTCHA、上传目录等敏感配置。
- 运行时需本地 MySQL 与 Redis，上传目录由 `file.upload-dir` 自动创建；生产环境建议开启对象存储或独立磁盘并配置备份/限流。
