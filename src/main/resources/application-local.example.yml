# Local profile example; copy to application-local.yml and adjust values, or override via environment variables.
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quickshare?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: quickshare
    password: change_me
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost
    port: 6379
    password:
    database: 0

  servlet:
    multipart:
      max-file-size: 1GB
      max-request-size: 1GB

  mail:
    host: smtp.gmail.com
    port: 587
    username: your-email@gmail.com
    password: your-app-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

server:
  port: 8080

file:
  upload-dir: /tmp/quickshare/uploads
  allowed-types:
  max-size: -1

jwt:
  secret: change-this-secret-at-least-256-bits
  expiration: 86400000

recaptcha:
  enabled: true
  site-key:
  secret-key:
  verify-url: https://www.google.com/recaptcha/api/siteverify

app:
  cors:
    allowed-origins: http://localhost:3000,http://localhost:8080
